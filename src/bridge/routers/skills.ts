// Re-export from split modules — kept for backward compatibility.
// New code should import from skills-router.ts or skills-handlers.ts directly.
export { skillsRouter, type SkillsDeps } from "./skills-router.js";
